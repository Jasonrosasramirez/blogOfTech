/* will work on models/Comments.js file. */

// importing the required file 
const router = require("express").Router(); // will be our route handler
const Comment = require("../../models/index.js"); // using the comment object after the index file has made edits to it


